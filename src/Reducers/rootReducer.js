const initialState = {

}

const rootReducer = (state = initialState, action) => {
  let newState = {...state};

  switch(action.type){
    default:
      ;
  }

  return newState;
}

export default rootReducer;
